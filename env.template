S3_BUCKET="your_bucket"
S3_REGION="default"
S3_ENDPOINT="https://***.com"
S3_CUSTOM_DOMAIN="https://***.com"
S3_ACCESS_KEY="your_key"
S3_SECRET_KEY="your_secret"
# [Config: Workspace Directory]
# Optional: Enforce a specific host directory to bind as the Agent's workspace (/share).
# If not set, it defaults to the current working directory where you run the command.
# HOST_WORK_DIR="/path/to/your/custom/workspace"
